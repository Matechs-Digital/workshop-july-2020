{
  "name": "@workshop/app",
  "version": "0.0.0",
  "scripts": {
    "start": "node index.js",
    "build": "rm -rf esm && tsc -p tsconfig.build.json",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "@workshop/common": "^0.0.0",
    "@workshop/components": "^0.0.0",
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  }
}
