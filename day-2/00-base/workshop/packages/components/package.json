{
  "name": "@workshop/components",
  "version": "0.0.0",
  "scripts": {
    "build": "rm -rf esm && tsc -p tsconfig.build.json",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "@workshop/common": "^0.0.0",
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  }
}
