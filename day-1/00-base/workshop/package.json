{
  "name": "workshop",
  "version": "0.0.0",
  "private": true,
  "workspaces": {
    "packages": [
      "./packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "rimraf": "^3.0.2"
  },
  "scripts": {
    "clean": "rimraf ./packages/*/node_modules && rimraf ./node_modules",
    "bootstrap": "lerna bootstrap"
  }
}
